modules:
  http_2xx:
    prober: http
    timeout: 5s
    http:
      valid_http_versions: ["HTTP/1.1", "HTTP/2.0"]
      no_follow_redirects: false
      fail_if_ssl: false
      preferred_ip_protocol: "ip4"
      headers:
        User-Agent: "HotelFlux-Blackbox/v1.0"
